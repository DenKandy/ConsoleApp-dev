namespace App.Commands
{
    enum EnCommands { Help, Exit, Clear, Alias, Set, Get, Remove, Change }
    enum EnParams { Help, Force, List, All, Set, Get, Remove, Change, Command }
    enum EnTags { Configuration, Use, Description, Parameters, Property, Alias }
}